"""Search Regn No — MMS Admin.

Endpoints, schemas and service logic to be provided.
"""

from fastapi import APIRouter

router = APIRouter(
    prefix="/admin/search-regn-no",
    tags=["admin: search regn no"],
)
