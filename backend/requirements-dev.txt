# MISO 5.0 — backend development / test dependencies.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=8.0
httpx>=0.27          # FastAPI TestClient
ruff>=0.4            # lint + format
mypy>=1.10           # type checking
