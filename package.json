{
  "name": "mms",
  "private": true,
  "description": "MMS monorepo — frontend (TanStack) + backend (FastAPI)",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend/mms && python -m uvicorn main:app --reload --port 8001",
    "build": "npm run build --prefix frontend",
    "build:dev": "npm run build:dev --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "format": "npm run format --prefix frontend"
  }
}
